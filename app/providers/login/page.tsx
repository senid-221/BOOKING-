"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";import {ArrowLeft,ArrowRight,Loader2,LockKeyhole} from "lucide-react";
export default function ProviderLogin(){const router=useRouter();const[phone,setPhone]=useState("");const[password,setPassword]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");
async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/providers/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({phone,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Login failed");router.replace("/provider");router.refresh()}catch(e){setError(e instanceof Error?e.message:"Login failed")}finally{setBusy(false)}}
return <main className="auth"><div className="auth-card"><Link href="/" className="auth-brand"><span className="mark"><LockKeyhole size={18}/></span>BOOKING PROVIDER</Link><div className="auth-copy"><small>PROVIDER LOGIN</small><h1>Injira</h1></div><form onSubmit={submit}><label>Phone<input required value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+250 7XX XXX XXX"/></label><label>Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label>{error&&<div className="login-error">{error}</div>}<button className="primary full" disabled={busy}>{busy?<Loader2 size={17} className="spin"/>:null} Login <ArrowRight size={17}/></button></form><Link href="/" className="back home-back"><ArrowLeft size={16}/> Back home</Link></div></main>}