import {NextResponse} from "next/server";import {PrismaClient} from "@prisma/client";const prisma=new PrismaClient();
export async function POST(req:Request){try{const{id,phone}=await req.json();if(!id||!phone)return NextResponse.json({error:"Booking reference and phone are required"},{status:400});const b=await prisma.booking.findUnique({where:{id},include:{service:true,provider:{select:{fullName:true}}}});if(!b||b.phone!==phone)return NextResponse.json({error:"Booking not found. Check your reference and phone."},{status:404});return NextResponse.json({id:b.id,customerName:b.customerName,phone:b.phone,service:b.service.name,provider:b.provider?.fullName||null,date:b.date,time:b.time,location:b.location,status:b.status,createdAt:b.createdAt});}catch(e){console.error(e);return NextResponse.json({error:"Unable to track booking"},{status:500})}}