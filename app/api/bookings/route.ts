import {NextResponse} from "next/server";import {PrismaClient} from "@prisma/client";const prisma=new PrismaClient();
export async function POST(req:Request){try{const b=await req.json();if(!b.customerName||!b.phone||!b.serviceName)return NextResponse.json({error:"Customer name, phone and service are required"},{status:400});const service=await prisma.service.findUnique({where:{name:b.serviceName}});if(!service)return NextResponse.json({error:"Service not found"},{status:404});const row=await prisma.booking.create({data:{customerName:b.customerName,phone:b.phone,serviceId:service.id,date:b.date||null,time:b.time||null,location:b.location||null,note:b.note||null,status:"PENDING"}});return NextResponse.json({id:row.id,status:row.status})}catch(e){console.error(e);return NextResponse.json({error:"Booking could not be created"},{status:500})}}