import {NextResponse} from "next/server";import {PrismaClient} from "@prisma/client";const prisma=new PrismaClient();
export async function GET(){const services=await prisma.service.findMany({orderBy:{name:"asc"}});const providers=await prisma.provider.findMany({where:{status:"ACTIVE"},include:{service:true},orderBy:{fullName:"asc"}});return NextResponse.json({services:services.map(s=>({id:s.id,name:s.name,monthlyFee:s.monthlyFee,maxProviders:s.maxProviders,active:providers.filter(p=>p.serviceId===s.id).length})),providers:providers.map(p=>({id:p.id,fullName:p.fullName,phone:p.phone,service:p.service.name}))})}