import {NextResponse} from "next/server";import {PrismaClient} from "@prisma/client";const prisma=new PrismaClient();export async function POST(req:Request){try{const{fullName,phone,message}=await req.json();if(!fullName||!phone||!message)return NextResponse.json({error:"All fields are required"},{status:400});const row=await prisma.contactMessage.create({data:{fullName,phone,message,status:"NEW"}});return NextResponse.json({id:row.id,status:row.status})}catch(e){console.error(e);return NextResponse.json({error:"Message could not be sent"},{status:500})}}