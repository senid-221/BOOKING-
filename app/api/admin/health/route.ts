import {NextResponse} from "next/server";import {cookies} from "next/headers";import {cookieName,isValidAdminSession} from "@/lib/admin-auth";import {PrismaClient} from "@prisma/client";const prisma=new PrismaClient();
export async function GET(){const c=await cookies();if(!isValidAdminSession(c.get(cookieName)?.value))return NextResponse.json({error:"Unauthorized"},{status:401});let db="OK";try{await prisma.$queryRaw`SELECT 1`}catch{db="ERROR"}return NextResponse.json({database:db,nodeEnv:process.env.NODE_ENV||"unknown",momoConfigured:Boolean(process.env.MOMO_API_USER&&process.env.MOMO_API_KEY&&process.env.MOMO_COLLECTION_PRIMARY_KEY),appUrlConfigured:Boolean(process.env.APP_URL)})}