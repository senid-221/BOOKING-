"use client";
import {useSearchParams} from "next/navigation";import Link from "next/link";import {ArrowLeft,ArrowRight,CalendarDays,MapPin} from "lucide-react";import {useState} from "react";
export default function Booking(){const p=useSearchParams();const service=p.get("service")||"Choose a service";const[name,setName]=useState("");const[phone,setPhone]=useState("");const[date,setDate]=useState("");const[time,setTime]=useState("");const[location,setLocation]=useState("");const[note,setNote]=useState("");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);
async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");try{const r=await fetch("/api/bookings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({customerName:name,phone,serviceName:service,date,time,location,note})});const d=await r.json();if(!r.ok)throw new Error(d.error);setMsg("Booking yawe yakiriwe. Reference: "+d.id);}catch(e){setMsg(e instanceof Error?e.message:"Booking failed")}finally{setBusy(false)}}
return <main className="page"><Link href="/services" className="back"><ArrowLeft size={17}/> Back to services</Link><div className="booking-layout"><section><small>NEW BOOKING</small><h1>{service}</h1><p className="lead">Uzuza amakuru akenewe kugirango utange booking yawe.</p><form className="form-card" onSubmit={submit}><label>Service<input value={service} readOnly/></label><div className="two"><label>Date<input required type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>Time<input required type="time" value={time} onChange={e=>setTime(e.target.value)}/></label></div><label>Location<div className="input-icon"><MapPin size={17}/><input required value={location} onChange={e=>setLocation(e.target.value)} placeholder="Kigali, Rwanda"/></div></label><label>Your name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Andika amazina yawe"/></label><label>WhatsApp / Phone<input required value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+250 7XX XXX XXX"/></label><label>Additional note<textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Andi makuru..." rows={4}/></label><button className="primary full" disabled={busy}>Confirm Booking <ArrowRight size={17}/></button>{msg&&<div className="admin-message">{msg}</div>}</form></section><aside className="booking-summary"><div className="summary-icon"><CalendarDays size={24}/></div><small>BOOKING SUMMARY</small><h2>{service}</h2><p>Booking yawe izabikwa muri system kandi admin ashobora kuyireba kuri dashboard.</p></aside></div></main>