"use client";import {useState} from "react";import Link from "next/link";import {ArrowLeft,ArrowRight,Mail,MessageCircle} from "lucide-react";
export default function Contact(){const[name,setName]=useState("");const[phone,setPhone]=useState("");const[message,setMessage]=useState("");const[sent,setSent]=useState(false);const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);try{const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({fullName:name,phone,message})});if(r.ok){setSent(true);setName("");setPhone("");setMessage("")}}finally{setLoading(false)}}return <main className="public-page contact-page"><Link href="/" className="back"><ArrowLeft size={17}/> Back home</Link><div className="contact-hero"><small>HELP & CONTACT</small><h1>Dufashe kubona service.</h1><p>Andika ikibazo cyangwa ubusabe bwawe; support team ishobora kugufasha.</p></div><div className="contact-grid"><form className="contact-card" onSubmit={submit}><label>Amazina<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Amazina yawe"/></label><label>Phone / WhatsApp<input required value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+250 7XX XXX XXX"/></label><label>Message<textarea required rows={6} value={message} onChange={e=>setMessage(e.target.value)} placeholder="Andika ikibazo cyawe..."/></label><button className="primary full" disabled={loading}><MessageCircle size={17}/> {loading?"Sending...":"Send message"} <ArrowRight size={17}/></button>{sent&&<div className="admin-message">Ubutumwa bwawe bwakiriwe. Support izabukurikirana.</div>}</form><aside className="contact-side"><div><Mail size={20}/><h3>Support</h3><p>Shaka ubufasha kuri booking, providers, payments cyangwa services.</p></div><div><MessageCircle size={20}/><h3>WhatsApp</h3><p>Ongeramo WhatsApp support number ya BOOKING muri environment/configuration mbere ya production.</p></div></aside></div></main>}