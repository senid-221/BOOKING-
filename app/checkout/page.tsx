"use client";
import {useSearchParams,useRouter} from "next/navigation";import {useEffect,useState} from "react";import Link from "next/link";import {ArrowLeft,ArrowRight,CheckCircle2,Loader2,MapPin,Phone,ShoppingBag} from "lucide-react";
export default function Checkout(){const p=useSearchParams();const router=useRouter();const service=p.get("service")||"";const[name,setName]=useState("");const[phone,setPhone]=useState("");const[location,setLocation]=useState("");const[note,setNote]=useState("");const[loading,setLoading]=useState(false);const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMsg("");try{const r=await fetch("/api/bookings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({customerName:name,phone,serviceName:service,date:p.get("date"),time:p.get("time"),location,note})});const d=await r.json();if(!r.ok)throw new Error(d.error);router.replace("/booking/success?id="+encodeURIComponent(d.id));}catch(e){setMsg(e instanceof Error?e.message:"Booking failed")}finally{setLoading(false)}}return <main className="page"><Link href={"/booking?service="+encodeURIComponent(service)} className="back"><ArrowLeft size={17}/> Back</Link><div className="checkout-layout"><section><small>CHECKOUT</small><h1>Confirm booking.</h1><p className="lead">Reba amakuru yawe mbere yo kohereza booking.</p><form className="form-card" onSubmit={submit}><div className="checkout-service"><span><ShoppingBag size={20}/></span><div><small>SERVICE</small><b>{service||"Service"}</b></div></div><label>Amazina<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Amazina yawe"/></label><label>Phone / WhatsApp<div className="input-icon"><Phone size={16}/><input required value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+250 7XX XXX XXX"/></div></label><label>Location<div className="input-icon"><MapPin size={16}/><input required value={location} onChange={e=>setLocation(e.target.value)} placeholder="Kigali, Rwanda"/></div></label><label>Note<textarea rows={4} value={note} onChange={e=>setNote(e.target.value)} placeholder="Andi makuru..."/></label><button className="primary full" disabled={loading}>{loading?<Loader2 className="spin" size={17}/>:<CheckCircle2 size={17}/>} Confirm Booking <ArrowRight size={17}/></button>{msg&&<div className="admin-message">{msg}</div>}</form></section><aside className="checkout-summary"><small>SUMMARY</small><h2>{service||"Your service"}</h2><p><b>Date:</b> {p.get("date")||"—"}</p><p><b>Time:</b> {p.get("time")||"—"}</p><p><b>Location:</b> {location||"Will be provided"}</p></aside></div></main>}