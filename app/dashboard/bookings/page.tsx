"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {ArrowLeft,CalendarCheck2,Clock3,MapPin,Search} from "lucide-react";
type Booking={id:string;customerName:string;phone:string;service:string;provider:string|null;date:string|null;time:string|null;location:string|null;status:string;createdAt:string};
export default function Bookings(){const[data,setData]=useState<Booking[]>([]);const[q,setQ]=useState("");useEffect(()=>{fetch("/api/admin/bookings",{cache:"no-store"}).then(r=>r.ok?r.json():[]).then(setData)},[]);const rows=data.filter(b=>(b.customerName+" "+b.phone+" "+b.service+" "+(b.provider||"")).toLowerCase().includes(q.toLowerCase()));return <main className="admin-page"><Link href="/dashboard" className="back"><ArrowLeft size={17}/> Dashboard</Link><div className="admin-page-head"><div><small>BOOKINGS</small><h1>Bookings</h1><p>Customer reservations and service requests.</p></div><div className="admin-search"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search booking..."/></div></div><div className="panel admin-table"><div className="table-wrap"><table><thead><tr><th>Customer</th><th>Service</th><th>Provider</th><th>Date / Time</th><th>Location</th><th>Status</th></tr></thead><tbody>{rows.map(b=><tr key={b.id}><td><b>{b.customerName}</b><small className="cell-small">{b.phone}</small></td><td>{b.service}</td><td>{b.provider||"Unassigned"}</td><td><span className="booking-meta"><CalendarCheck2 size={13}/>{b.date||"—"} <Clock3 size={13}/>{b.time||"—"}</span></td><td><span className="booking-meta"><MapPin size={13}/>{b.location||"—"}</span></td><td><span className={"status "+b.status.toLowerCase()}>{b.status}</span></td></tr>)}</tbody></table></div></div></main>