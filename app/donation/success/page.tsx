"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {ArrowLeft,CheckCircle2,Clock3,HeartHandshake,RefreshCw,XCircle} from "lucide-react";import {useSearchParams} from "next/navigation";
export default function DonationResult(){const p=useSearchParams();const id=p.get("donationId");const[status,setStatus]=useState("PENDING");const[amount,setAmount]=useState<number|null>(null);const[reference,setReference]=useState<string|null>(null);const[loading,setLoading]=useState(true);
async function check(){if(!id){setLoading(false);return}const r=await fetch("/api/donations/"+encodeURIComponent(id),{cache:"no-store"});if(r.ok){const d=await r.json();setStatus(d.status);setAmount(d.amount);setReference(d.reference)}}useEffect(()=>{check();const timer=setInterval(()=>{if(status==="PENDING")check()},5000);return()=>clearInterval(timer)},[id,status]);
const money=amount?new Intl.NumberFormat("en-RW").format(amount)+" RWF":"";
return <main className="page result-page"><div className="result-card"><div className={"result-icon "+status.toLowerCase()}>{status==="SUCCESS"?<CheckCircle2 size={34}/>:status==="FAILED"?<XCircle size={34}/>:<Clock3 size={34}/>}</div><small>DONATION STATUS</small><h1>{status==="SUCCESS"?"Murakoze cyane!":status==="FAILED"?"Payment ntiyagenze neza.":"Tegereza gato..."}</h1><p>{status==="SUCCESS"?"Ubufasha bwawe bwakiriwe kandi bwemejwe.":status==="FAILED"?"Payment yanze cyangwa yarahagaritswe. Ongera ugerageze.":"MTN MoMo payment iracyategereje kwemezwa kuri telefoni yawe."}</p>{amount&&<div className="result-amount">{money}</div>}{reference&&<div className="reference"><span>Reference</span><code>{reference}</code></div>}{status==="PENDING"&&<button className="secondary refresh" onClick={check}><RefreshCw size={16}/> Check status</button>}<div className="result-actions"><Link className="primary" href={status==="FAILED"?"/donation":"/"}>{status==="FAILED"?"Try again":"Back home"} <ArrowLeft size={16}/></Link><Link className="secondary" href="/donation"><HeartHandshake size={16}/> Donate again</Link></div></div></main>}